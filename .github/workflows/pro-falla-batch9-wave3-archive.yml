name: Pro Falla batch 9 wave 3 original SPA localization

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/pro-falla-batch9-wave3-archive.yml'
      - 'tools/pro-falla-batch9-wave3/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archive:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/pro-falla-batch9-20260825
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          set -Eeuo pipefail
          npm install playwright@1.54.2 --no-save
          npx playwright install --with-deps chromium
          python -m pip install --disable-pip-version-check pillow
          sudo apt-get update -qq
          sudo apt-get install -y -qq fonts-noto-cjk
      - name: Generate archiver
        run: |
          set -Eeuo pipefail
          python -m py_compile tools/pro-falla-batch9-wave3/make_archiver.py
          python tools/pro-falla-batch9-wave3/make_archiver.py
          python -m py_compile tools/pro-falla-batch9-wave3/_builder_generated.py
          python tools/pro-falla-batch9-wave3/_builder_generated.py
          node --check tools/pro-falla-batch9-wave3/archive_wave3.mjs
      - name: Capture and replay original Falla SPAs
        env:
          OUT_DIR: ${{ github.workspace }}/artifacts/pro-falla-batch9-wave3-original-spa
          ONLINE_WAIT_MS: '6500'
          OFFLINE_WAIT_MS: '5200'
          PAGE_CONCURRENCY: '3'
        run: |
          set -Eeuo pipefail
          node tools/pro-falla-batch9-wave3/archive_wave3.mjs
      - name: Build contact sheets
        env:
          OUT_DIR: ${{ github.workspace }}/artifacts/pro-falla-batch9-wave3-original-spa
        run: |
          set -Eeuo pipefail
          python -m py_compile tools/pro-falla-batch4/contact_sheets_original.py
          python tools/pro-falla-batch4/contact_sheets_original.py
      - name: Print QA
        if: always()
        run: |
          cat artifacts/pro-falla-batch9-wave3-original-spa/QA-FINAL.md >> "$GITHUB_STEP_SUMMARY" || true
          du -sh artifacts/pro-falla-batch9-wave3-original-spa || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pro-falla-batch9-wave3-original-spa
          path: artifacts/pro-falla-batch9-wave3-original-spa
          if-no-files-found: error
          retention-days: 14
          compression-level: 6
