name: Temporary Pro-BIGO Rich Probe

on:
  push:
    branches:
      - main
    paths:
      - 'tools/pro-bigo/**'
      - '.github/workflows/tmp-pro-bigo-rich-probe.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install browser runtime
        run: |
          npm init -y
          npm install playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Probe dedicated SPAs
        run: node tools/pro-bigo/probe.mjs
      - name: Upload probe artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pro-bigo-rich-probe
          path: pro-bigo-rich-probe-output
          if-no-files-found: error
          compression-level: 4
          retention-days: 14
      - name: Publish stable probe branch
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/pro-bigo-rich-probe-results
          mkdir -p /tmp/pro-bigo-rich-probe-results
          if [ -d pro-bigo-rich-probe-output ]; then cp -R pro-bigo-rich-probe-output/. /tmp/pro-bigo-rich-probe-results/; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+moongrant@users.noreply.github.com"
          git checkout --orphan pro-bigo-rich-probe-results-tmp
          git rm -rf . >/dev/null 2>&1 || true
          find . -mindepth 1 -maxdepth 1 -not -name .git -exec rm -rf {} +
          cp -R /tmp/pro-bigo-rich-probe-results/. .
          printf '# Pro-BIGO rich probe results\n' > README.md
          git add -A
          git commit -m "Update Pro-BIGO rich probe results"
          git push origin HEAD:pro-bigo-rich-probe-results --force
