name: Yalla evidence-backed uncollected root probe

on:
  push:
    branches: [main]
    paths:
      - tools/yalla-next-batch/evidence_backed_probe.py
      - .github/workflows/yalla-evidence-backed-probe.yml
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  probe:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    env:
      RESULT_BRANCH: pro-yalla-evidence-backed-probe-results
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install and run strict probe
        run: |
          python -m pip install requests beautifulsoup4 lxml playwright==1.55.0
          python -m playwright install --with-deps chromium
          python -m py_compile tools/yalla-next-batch/evidence_backed_probe.py
          rm -rf pro-yalla-evidence-backed-probe
          python tools/yalla-next-batch/evidence_backed_probe.py 2>&1 | tee yalla-evidence-probe.log
      - name: Publish results
        if: always()
        run: |
          set -Eeuo pipefail
          stage=/tmp/yalla-evidence-probe
          rm -rf "$stage" && mkdir -p "$stage"
          cp -R pro-yalla-evidence-backed-probe/. "$stage/" 2>/dev/null || true
          cp yalla-evidence-probe.log "$stage/" 2>/dev/null || true
          git config user.name 'pro-yalla-probe-bot'
          git config user.email '53897102+moongrant@users.noreply.github.com'
          git switch --orphan "$RESULT_BRANCH"
          git rm -rf . >/dev/null 2>&1 || true
          cp -a "$stage"/. .
          git add .
          git commit -m "Publish evidence-backed Yalla root probe ${GITHUB_RUN_ID}"
          git push --force origin "$RESULT_BRANCH"
      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pro-yalla-evidence-backed-probe
          path: pro-yalla-evidence-backed-probe
          if-no-files-found: warn
          compression-level: 0
          retention-days: 30
